### Functions

- Running the same set of stored instructions repeatedly without requiring you to adjust the settings every time.
- One of the basic principles of programming can be summed with the acronym DRY in other words don't repeat yourself.
- With functions you can take several lines of code that performs a set of related actions and then group them together under a single label.
- Then when you need to run the code that you've saved, you just invoke or call the function. You can run the code as many times as you want

```jsx
// Add two numbers
var a = 10;
var b = 20;
car c = a + b;
console.log(c)

// Declaring a function
function addTwoNum(){
	// code goes here -> body of the function
}
```

- It's important to remember that a function declaration on its own doesn't execute the code.
- It's just specifying what operations should be performed. Once the function is executed to run the functions code you need to call or invoke the function

```jsx
// call or execute a function
addTwoNum();
```

Flexible function

```jsx
function addTwoNum(a, b) {
  var c = a + b;
  console.log(c);
}

addTwoNum(2, 2);
```

- a & b are function parameter and are placed on the function definition.
- The value passed to the function are called arguments

---

### Storing data in arrays

### Building and calling functions

### Exercise: Practicing with functions

### Solution: Practicing with functions

### Self review: Practicing with functions

### Introduction to objects

### Object Literals and the Dot Notation

### Object Literals and the Brackets Notation

### Arrays are Objects

### Math object cheat sheet

### Math object

### A closer look at strings

### String cheat sheet

### Exercise: Creating arrays and objects

### Creating arrays and objects (solutions)

### Self review: Creating arrays and objects

### Object Methods

### Typeof

### JavaScript arrays, objects and functions

### Knowledge check: Arrays, Objects and Functions

### Additional resources
